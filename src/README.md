# src

**Purpose:** Python package source root. The installable `fraud-scoring-engine` package lives in the `fraud_scoring_engine` subfolder (configured via `package-dir` in [pyproject.toml](../pyproject.toml)).

## Contents

- `fraud_scoring_engine/` - main library package (see [fraud_scoring_engine/README.md](fraud_scoring_engine/README.md))

## Related

- [Root README](../README.md)
- [scripts/README.md](../scripts/README.md) - CLI entry points that import from this package
